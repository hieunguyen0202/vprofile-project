# Copy to `application-secrets.properties` (not committed) and fill in values.
#
# This file is intentionally excluded from git. It is loaded automatically (if present)
# by `src/main/webapp/WEB-INF/appconfig-root.xml`.

jdbc.password=
rabbitmq.username=
rabbitmq.password=
